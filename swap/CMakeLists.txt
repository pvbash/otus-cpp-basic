cmake_minimum_required(VERSION 3.8)
project(swap CXX)
add_executable(swap swap.cpp)
target_compile_features(swap PUBLIC cxx_std_17)
if(MSVC)
    target_compile_options(swap PUBLIC /W4 /WX)
else()
    target_compile_options(swap PUBLIC -Wall -Wextra -Werror -pedantic)
endif()